# This repo doubles as Lars's own live, installed instance of the tool. His actual research
# content and runtime state must never ship in the public source tree — only the skills,
# CLAUDE.md runbook, KNOBS.md, and OKF-SPEC.md do.
/research/
/autowikipathy/

# internal tracker - never publish (auto-committed by mistake 2026-07-10, removed)
/TODO.md
